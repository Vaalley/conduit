# Passport menu

`/passport` opens a read-only 27-slot passport chest for the sender, while
`/passport <player>` opens the selected player's passport and `/passport text`
retains the legacy text representation. The stamps page lists every canonical
stamp with locked/unlocked state and pagination.
