# 06 — Parity sweep and docs

**What to build:** Walk the whole issue-#40 list against the implemented
behaviour and its gametests, fill any gaps, and document the two new flags.

**Blocked by:** 01–05.

**Status:** blocked

- [ ] Every row of `../spec.md`'s tables has a gametest or a cited reason it
      does not need one.
- [ ] `CONTEXT.md` glossary entries for `BOAT` and `MINECART` (if not already
      landed in ticket 03), and any new vocabulary the classifier introduced.
- [ ] `docs/research/portal-feature-inventory.md` §2.8 flag list and protection
      notes updated to describe the new interaction verdict as a deliberate
      port-era departure.
- [ ] A short `docs/` note (or an ADR if the classifier turns out to be a
      structural decision) on the FREE / CONTAINER_VIEW / REQUIRES_MEMBERSHIP /
      CAMPFIRE model.
- [ ] Final `/code-review` of the whole branch against `../spec.md`.

## Comments
