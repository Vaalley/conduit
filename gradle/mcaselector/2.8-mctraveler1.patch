diff --git a/build.gradle b/build.gradle
index a5221320..0d9e57a3 100644
--- a/build.gradle
+++ b/build.gradle
@@ -98,6 +98,12 @@ jar {
 }
 
 shadowJar {
+	// Build the same bytes every time, so the jar can be pinned by checksum by anyone who
+	// builds it rather than only by whoever uploaded it. Without these two, the archive
+	// carries the build's wall clock and the filesystem's directory order, and two builds
+	// of identical source hash differently.
+	preserveFileTimestamps = false
+	reproducibleFileOrder = true
 	minimize {
 		exclude(dependency('org.apache.logging.log4j:log4j-core:.*'))
 		exclude(dependency('org.apache.groovy:groovy-jsr223:.*'))
diff --git a/src/main/java/net/querz/mcaselector/selection/Selection.java b/src/main/java/net/querz/mcaselector/selection/Selection.java
index 5e453348..22ea66f5 100644
--- a/src/main/java/net/querz/mcaselector/selection/Selection.java
+++ b/src/main/java/net/querz/mcaselector/selection/Selection.java
@@ -72,7 +72,14 @@ public class Selection implements Serializable, Iterable<Long2ObjectMap.Entry<Ch
 		return selection;
 	}
 
-	public void saveToFile(File csvFile) throws IOException {
+	/**
+	 * Synchronized for the same reason as {@link #merge(Selection)}, and not only to keep
+	 * the map from being read while it is written: acquiring the same monitor after the
+	 * last merge released it is what guarantees this thread sees every merge, rather than
+	 * whichever of them happen to have reached main memory. The CLI writes the selection
+	 * from whichever worker thread finished last, so it is not the thread that merged last.
+	 */
+	public synchronized void saveToFile(File csvFile) throws IOException {
 		try (BufferedWriter bw = new BufferedWriter(new FileWriter(csvFile))) {
 			if (inverted) {
 				bw.write("inverted\n");
@@ -95,7 +102,8 @@ public class Selection implements Serializable, Iterable<Long2ObjectMap.Entry<Ch
 		}
 	}
 
-	public String saveToString() {
+	/** Synchronized for the same reason as {@link #saveToFile(File)}. */
+	public synchronized String saveToString() {
 		StringBuilder sb = new StringBuilder();
 		if (inverted) {
 			sb.append("inverted\n");
@@ -429,7 +437,23 @@ public class Selection implements Serializable, Iterable<Long2ObjectMap.Entry<Ch
 		return result;
 	}
 
-	public void merge(Selection other) {
+	/**
+	 * Merges {@code other} into this selection.
+	 *
+	 * <p>Synchronized because this is called concurrently. Both {@code ParamExecutor.select}
+	 * and the UI hand {@code Selection::merge} to
+	 * {@link net.querz.mcaselector.io.job.ChunkFilterSelector#selectFilter} as the
+	 * per-region-file callback, so every job in the process pool invokes it on the same
+	 * target selection. {@link #selection} is a {@link Long2ObjectOpenHashMap}, which is not
+	 * thread safe: two jobs rehashing it at once dropped entries, so a select silently
+	 * returned fewer chunks than it matched. It went unnoticed because it loses whole
+	 * regions rather than corrupting the map, and only on some runs — measured at roughly
+	 * one run in twenty at the default thread count, and never with {@code --process-threads 1}.
+	 *
+	 * <p>Only {@code other} is read here, and it is a selection the calling job just built
+	 * for itself, so nothing locks two selections at once.
+	 */
+	public synchronized void merge(Selection other) {
 		if (!inverted && !other.inverted) {
 
 			for (Long2ObjectMap.Entry<ChunkSet> entry : other.selection.long2ObjectEntrySet()) {
diff --git a/src/main/java/net/querz/mcaselector/version/java_1_14/ChunkFilter_19w11a.java b/src/main/java/net/querz/mcaselector/version/java_1_14/ChunkFilter_19w11a.java
index 86155618..ad6519ab 100644
--- a/src/main/java/net/querz/mcaselector/version/java_1_14/ChunkFilter_19w11a.java
+++ b/src/main/java/net/querz/mcaselector/version/java_1_14/ChunkFilter_19w11a.java
@@ -8,6 +8,7 @@ import net.querz.nbt.CompoundTag;
 import net.querz.nbt.DoubleTag;
 import net.querz.nbt.IntArrayTag;
 import net.querz.nbt.ListTag;
+import net.querz.nbt.Tag;
 
 import static net.querz.mcaselector.util.validation.ValidationHelper.attempt;
 
@@ -103,11 +104,34 @@ public class ChunkFilter_19w11a {
 			Helper.fixEntityUUID(entity);
 		}
 
+		/**
+		 * Applies the offset to a villager's memory of a place.
+		 *
+		 * <p>A memory is serialized through {@code ExpirableValue}, which wraps whatever it
+		 * holds in {@code value}, so the position of a {@code minecraft:home},
+		 * {@code minecraft:job_site} or {@code minecraft:meeting_point} sits one level below
+		 * the memory rather than on it. Looking only at the memory left every such villager
+		 * remembering where it used to live.
+		 *
+		 * <p>Both shapes are offset rather than one replacing the other. A world is a mixture
+		 * of DataVersions, because a chunk is only rewritten when something loads it, so a
+		 * save can hold memories in either shape at the same time and a fix that swapped one
+		 * for the other would strand the chunks nobody has visited.
+		 */
 		protected void applyOffsetToVillagerMemory(CompoundTag memory, Point3i offset) {
-			IntArrayTag mPos = Helper.tagFromCompound(memory, "pos");
+			applyOffsetToMemoryPos(memory, offset);
+			Tag value = Helper.tagFromCompound(memory, "value");
+			if (value instanceof CompoundTag wrapped) {
+				applyOffsetToMemoryPos(wrapped, offset);
+			}
+		}
+
+		/** The {@code pos} directly on {@code holder}, in either of the shapes it is written in. */
+		private void applyOffsetToMemoryPos(CompoundTag holder, Point3i offset) {
+			IntArrayTag mPos = Helper.tagFromCompound(holder, "pos");
 			Helper.applyOffsetToIntArrayPos(mPos, offset);
 			if (mPos == null) {
-				ListTag lPos = Helper.tagFromCompound(memory, "pos");
+				ListTag lPos = Helper.tagFromCompound(holder, "pos");
 				Helper.applyOffsetToIntListPos(lPos, offset);
 			}
 		}
diff --git a/src/main/java/net/querz/mcaselector/version/java_1_18/ChunkFilter_21w37a.java b/src/main/java/net/querz/mcaselector/version/java_1_18/ChunkFilter_21w37a.java
index 01bc608f..a4bfb1ee 100644
--- a/src/main/java/net/querz/mcaselector/version/java_1_18/ChunkFilter_21w37a.java
+++ b/src/main/java/net/querz/mcaselector/version/java_1_18/ChunkFilter_21w37a.java
@@ -544,6 +544,49 @@ public class ChunkFilter_21w37a {
 	@MCVersionImplementation(2834)
 	public static class RelocateEntities extends ChunkFilter_20w45a.RelocateEntities {
 
+		/**
+		 * Every block position Minecraft writes as an int array of three at the root of an
+		 * entity, enumerated from Minecraft 26.2's own entity classes rather than from
+		 * recollection: each class that serialises a {@code BlockPos} through
+		 * {@code BlockPos.CODEC}.
+		 *
+		 * <ul>
+		 *   <li>{@code block_pos} — {@code BlockAttachedEntity}: item frame, glow item
+		 *       frame, painting, leash knot</li>
+		 *   <li>{@code sleeping_pos} — {@code LivingEntity}: anything that sleeps in a bed</li>
+		 *   <li>{@code home_pos} — {@code Mob}: a turtle's home, a creaking's, a happy
+		 *       ghast's, and any mob restricted to one</li>
+		 *   <li>{@code hive_pos}, {@code flower_pos} — {@code Bee}</li>
+		 *   <li>{@code anchor_pos} — {@code Phantom}</li>
+		 *   <li>{@code bound_pos} — {@code Vex}</li>
+		 *   <li>{@code beam_target} — {@code EndCrystal}</li>
+		 *   <li>{@code wander_target} — {@code WanderingTrader}</li>
+		 *   <li>{@code patrol_target} — {@code PatrollingMonster}: pillager, vindicator,
+		 *       evoker, illusioner, ravager, witch</li>
+		 * </ul>
+		 *
+		 * <p>{@code current_explosion_impact_pos} is deliberately not here. It is the only
+		 * other position {@code LivingEntity} persists, and it is a {@code Vec3} of doubles
+		 * rather than a block position — knockback state rather than a place anything
+		 * stands.
+		 *
+		 * <p>Keyed by name because these names are unambiguous, which the legacy triples are
+		 * not: every entity carrying {@code hive_pos} means the same thing by it, so this
+		 * list does not have to be crossed with the list of entity ids that have one.
+		 */
+		protected static final String[] INLINE_BLOCK_POSITIONS = {
+			"block_pos",
+			"sleeping_pos",
+			"home_pos",
+			"hive_pos",
+			"flower_pos",
+			"anchor_pos",
+			"bound_pos",
+			"beam_target",
+			"wander_target",
+			"patrol_target",
+		};
+
 		@Override
 		protected void applyOffsetToEntity(CompoundTag entity, Point3i offset) {
 			if (entity == null) {
@@ -557,9 +600,12 @@ public class ChunkFilter_21w37a {
 				entityPos.set(2, DoubleTag.valueOf(entityPos.getDouble(2) + offset.getZ()));
 			}
 
-			// leashed entities
+			// leashed entities, in both the compound and the inlined spelling. This
+			// implementation covers up to 4421, which includes the 1.21.5 releases that
+			// renamed Leash to leash and made it an int array.
 			CompoundTag leash = Helper.tagFromCompound(entity, "Leash");
 			Helper.applyIntOffsetIfRootPresent(leash, "X", "Y", "Z", offset);
+			Helper.applyOffsetToIntArrayPos(entity.getIntArrayTag("leash"), offset);
 
 			// projectiles
 			if (attempt(() -> Helper.applyIntOffsetIfRootPresent(entity, "xTile", "yTile", "zTile", offset))) {
@@ -569,6 +615,34 @@ public class ChunkFilter_21w37a {
 			// entities that have a sleeping place
 			Helper.applyIntOffsetIfRootPresent(entity, "SleepingX", "SleepingY", "SleepingZ", offset);
 
+			// The inlined block positions, keyed by name rather than by entity id. Each of
+			// these names a place on whatever entity carries it — a bee's hive, a phantom's
+			// anchor, the block an item frame hangs on — so one loop reaches every entity
+			// that has one, including a kind nobody thought to list. The legacy CamelCase
+			// triples stay in the switch below, because those spellings really are
+			// entity-specific: AX/AY/AZ means something only on a phantom. Both are applied,
+			// never one instead of the other, because a save holds chunks at a mixture of
+			// DataVersions. applyOffsetToIntArrayPos only touches an array of exactly three,
+			// so a key that happens to hold a uuid is left alone.
+			for (String key : INLINE_BLOCK_POSITIONS) {
+				Helper.applyOffsetToIntArrayPos(entity.getIntArrayTag(key), offset);
+			}
+
+			// Every memory in the brain, rather than the three a villager claims a point of
+			// interest with. A memory that holds a place holds it under "pos", either on the
+			// memory itself or inside the "value" that ExpirableValue wraps it in, so one
+			// loop reaches a villager's potential_job_site and an allay's liked_noteblock as
+			// readily as its home. A memory whose value is a *list* of places is not covered
+			// here; the sniffer case below is the one of those upstream knows about.
+			CompoundTag brainMemories = Helper.tagFromCompound(Helper.tagFromCompound(entity, "Brain"), "memories");
+			if (brainMemories != null) {
+				for (Tag memory : brainMemories.values()) {
+					if (memory instanceof CompoundTag m) {
+						Relocate.instance.applyOffsetToVillagerMemory(m, offset);
+					}
+				}
+			}
+
 			// positions for specific entity types
 			String id = Helper.stringFromCompound(entity, "id", "");
 			switch (id) {
@@ -603,17 +677,15 @@ public class ChunkFilter_21w37a {
 				Helper.applyIntOffsetIfRootPresent(beamTarget, "X", "Y", "Z", offset);
 				break;
 			case "minecraft:item_frame":
+			case "minecraft:glow_item_frame":
 			case "minecraft:painting":
+			case "minecraft:leash_knot":
+				// The block the thing hangs on, in the spelling used before 1.21.5 inlined
+				// it. All four are BlockAttachedEntity, which is why the glow item frame and
+				// the leash knot belong here beside the two upstream listed; the inlined
+				// block_pos they all now write is applied above rather than here.
 				Helper.applyIntOffsetIfRootPresent(entity, "TileX", "TileY", "TileZ", offset);
 				break;
-			case "minecraft:villager":
-				CompoundTag memories = Helper.tagFromCompound(Helper.tagFromCompound(entity, "Brain"), "memories");
-				if (memories != null && !memories.isEmpty()) {
-					Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:meeting_point"), offset);
-					Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:home"), offset);
-					Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:job_site"), offset);
-				}
-				break;
 			case "minecraft:pillager":
 			case "minecraft:witch":
 			case "minecraft:vindicator":
diff --git a/src/main/java/net/querz/mcaselector/version/java_1_21/ChunkFilter_25w15a.java b/src/main/java/net/querz/mcaselector/version/java_1_21/ChunkFilter_25w15a.java
index 3a7d483d..f8985cf1 100644
--- a/src/main/java/net/querz/mcaselector/version/java_1_21/ChunkFilter_25w15a.java
+++ b/src/main/java/net/querz/mcaselector/version/java_1_21/ChunkFilter_25w15a.java
@@ -30,9 +30,12 @@ public class ChunkFilter_25w15a {
 				entityPos.set(2, DoubleTag.valueOf(entityPos.getDouble(2) + offset.getZ()));
 			}
 
-			// leashed entities
+			// leashed entities, in both the compound and the inlined spelling. 1.21.5's
+			// InlineBlockPosFormatFix renamed Leash to leash and made it an int array; a
+			// chunk nothing has loaded since still carries the old one, so both are moved.
 			CompoundTag leash = Helper.tagFromCompound(entity, "Leash");
 			Helper.applyIntOffsetIfRootPresent(leash, "X", "Y", "Z", offset);
+			Helper.applyOffsetToIntArrayPos(entity.getIntArrayTag("leash"), offset);
 
 			// projectiles
 			if (attempt(() -> Helper.applyIntOffsetIfRootPresent(entity, "xTile", "yTile", "zTile", offset))) {
@@ -42,6 +45,34 @@ public class ChunkFilter_25w15a {
 			// entities that have a sleeping place
 			Helper.applyIntOffsetIfRootPresent(entity, "SleepingX", "SleepingY", "SleepingZ", offset);
 
+			// The inlined block positions, keyed by name rather than by entity id. Each of
+			// these names a place on whatever entity carries it — a bee's hive, a phantom's
+			// anchor, the block an item frame hangs on — so one loop reaches every entity
+			// that has one, including a kind nobody thought to list. The legacy CamelCase
+			// triples stay in the switch below, because those spellings really are
+			// entity-specific: AX/AY/AZ means something only on a phantom. Both are applied,
+			// never one instead of the other, because a save holds chunks at a mixture of
+			// DataVersions. applyOffsetToIntArrayPos only touches an array of exactly three,
+			// so a key that happens to hold a uuid is left alone.
+			for (String key : INLINE_BLOCK_POSITIONS) {
+				Helper.applyOffsetToIntArrayPos(entity.getIntArrayTag(key), offset);
+			}
+
+			// Every memory in the brain, rather than the three a villager claims a point of
+			// interest with. A memory that holds a place holds it under "pos", either on the
+			// memory itself or inside the "value" that ExpirableValue wraps it in, so one
+			// loop reaches a villager's potential_job_site and an allay's liked_noteblock as
+			// readily as its home. A memory whose value is a *list* of places is not covered
+			// here; the sniffer case below is the one of those upstream knows about.
+			CompoundTag brainMemories = Helper.tagFromCompound(Helper.tagFromCompound(entity, "Brain"), "memories");
+			if (brainMemories != null) {
+				for (Tag memory : brainMemories.values()) {
+					if (memory instanceof CompoundTag m) {
+						Relocate.instance.applyOffsetToVillagerMemory(m, offset);
+					}
+				}
+			}
+
 			// positions for specific entity types
 			String id = Helper.stringFromCompound(entity, "id", "");
 			switch (id) {
@@ -76,17 +107,15 @@ public class ChunkFilter_25w15a {
 					Helper.applyIntOffsetIfRootPresent(beamTarget, "X", "Y", "Z", offset);
 					break;
 				case "minecraft:item_frame":
+				case "minecraft:glow_item_frame":
 				case "minecraft:painting":
+				case "minecraft:leash_knot":
+					// The block the thing hangs on, in the spelling used before 1.21.5
+					// inlined it. All four are BlockAttachedEntity, which is why the glow
+					// item frame and the leash knot belong here beside the two upstream
+					// listed; the inlined block_pos they all now write is applied above.
 					Helper.applyIntOffsetIfRootPresent(entity, "TileX", "TileY", "TileZ", offset);
 					break;
-				case "minecraft:villager":
-					CompoundTag memories = Helper.tagFromCompound(Helper.tagFromCompound(entity, "Brain"), "memories");
-					if (memories != null && !memories.isEmpty()) {
-						Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:meeting_point"), offset);
-						Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:home"), offset);
-						Relocate.instance.applyOffsetToVillagerMemory(Helper.tagFromCompound(memories, "minecraft:job_site"), offset);
-					}
-					break;
 				case "minecraft:pillager":
 				case "minecraft:witch":
 				case "minecraft:vindicator":
@@ -114,9 +143,9 @@ public class ChunkFilter_25w15a {
 						}
 					}
 					break;
-				case "minecraft:happy_ghast":
-					Helper.applyOffsetToIntArrayPos(entity.getIntArrayTag("home_pos"), offset);
-					break;
+				// minecraft:happy_ghast had a case here for home_pos. It is applied above
+				// now, for every mob that has one rather than only this one, and applying
+				// it in both places would move the ghast's home twice.
 			}
 
 			// recursively update passengers
@@ -149,11 +178,20 @@ public class ChunkFilter_25w15a {
 
 	protected static class Relocate extends ChunkFilter_21w06a.Relocate {
 
-		static Relocate instance;
-
-		public Relocate() {
-			instance = this;
-		}
+		/**
+		 * Constructed here rather than by {@code VersionHandler}, which only instantiates the
+		 * classes it registers and so left this field null — and {@link RelocateEntities}
+		 * above dereferences it for every entity it relocates. The NPE was swallowed by the
+		 * per-entity {@code catchAndLog}, so a relocation reported success having silently
+		 * abandoned each entity partway through: villagers kept the memories they had before
+		 * the move, and falling blocks kept their tile entity data.
+		 *
+		 * <p>Registering the class instead would be wrong. It extends a 1.17-era
+		 * {@code Relocate}, so annotating it would also make it the <em>terrain</em>
+		 * implementation from 4422 onwards, ahead of {@code ChunkFilter_24w10a.Relocate}.
+		 * It exists only so {@link RelocateEntities} can reach the inherited helpers.
+		 */
+		static Relocate instance = new Relocate();
 
 		@Override
 		protected void applyOffsetToVillagerMemory(CompoundTag memory, Point3i offset) {
