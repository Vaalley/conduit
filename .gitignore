# Gradle
.gradle/
build/

# Fabric Loom local cache
.fabric/

# Dev/test/smoke server run dirs and stray server logs
run/
logs/

# MCA Selector's log4j config names its log directory with a property nothing sets,
# so every merge test that runs the tool leaves a literal ${sys:logDir} behind in
# whatever directory it was started from. Harmless, and not ours to configure.
${sys:logDir}/

# IDE
.idea/
*.iml
.vscode/
out/
classes/

# OS
.DS_Store
.claude/worktrees/

# Personal Claude Code overrides (machine-specific paths)
.claude/settings.local.json
